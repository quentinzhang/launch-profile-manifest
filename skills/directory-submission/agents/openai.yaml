interface:
  display_name: "Directory Submission"
  short_description: "Map product documents into reviewed directory tasks"
  default_prompt: "Use $directory-submission to extract my product facts, confirm uncertain fields, and create a validated directory submission Browser Task."
policy:
  allow_implicit_invocation: true
