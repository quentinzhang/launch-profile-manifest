interface:
  display_name: "Directory Submission"
  short_description: "Prepare reviewed directory listing browser tasks"
  default_prompt: "Use $directory-submission to turn my confirmed launch profile into a validated directory submission Browser Task."
policy:
  allow_implicit_invocation: true
